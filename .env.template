# Exchange Mode Configuration
# Options: mock, ccxt, binance
# - mock: Completely offline, no API calls (RECOMMENDED FOR TESTING)
# - ccxt: CCXT library with real Binance API (requires valid API keys)
# - binance: Legacy Binance REST API (requires valid API keys)
EXCHANGE_MODE=mock

# Binance API Credentials (only needed if EXCHANGE_MODE=ccxt or binance)
# Get testnet credentials from: https://testnet.binancefuture.com
# IMPORTANT: Replace the values below with YOUR actual API keys
BINANCE_API_KEY=YOUR_API_KEY_HERE
BINANCE_API_SECRET=YOUR_API_SECRET_HERE

# CCXT Sandbox Mode (only used if EXCHANGE_MODE=ccxt)
# True = Testnet (safe), False = Live trading (real money!)
CCXT_SANDBOX_MODE=True
